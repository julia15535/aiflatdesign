# PROJECT-STATE — текущее состояние проекта

Один файл — одна правда о том где мы сейчас. Обновляется в конце каждого дня / при больших изменениях.

**Последнее обновление**: 2026-05-11

## Где мы сейчас

**Фаза**: Шаг 3.5 (двухфазная генерация) — в работе.

**День проекта**: 3-4 из 8.

**Что в работе прямо сейчас**: pipeline разделён на этапы А (удаление + размеры) + Б (финальная вставка). Свободный текст вместо словаря. Retry при скачивании. См. DECISIONS.md 2026-05-11.

**Что готово после Шага 0**:
- Память проекта в `.memory_bank/` полная (включая аудит концепций референса)
- Репозиторий на Github: `julia15535/aiflatdesign`, коммит `bd1a726` запушен
- Папка на сервере `/opt/aiflatdesigner/` склонирована и готова принять код
- План шага 0 в `completed_plans/day_0_setup_and_audit.md`
- Claude настроен в режиме полного авто-выполнения (`bypassPermissions`)

## Что уже сделано

### Память проекта

- ✅ Полная структура `.memory_bank/`: `CLAUDE.md`, `README.md`, `product_brief.md`, `product_roadmap.md`
- ✅ `domain/` — pipeline, states, data_model
- ✅ `ai/` — все Stage'и 0-5 + openai_vision, replicate, prompts
- ✅ `bot/` — flow, onboarding, commands, handlers
- ✅ `infra/` — server, docker, github, secrets_template
- ✅ `_claude/` — DECISIONS, DEPLOY, SECRETS, workflow
- ✅ `business/budget.md`
- 🔄 `guides/` (новые): plan_template, documentation_style, lang — в процессе создания (Шаг 0)
- 🔄 `_claude/INDEX.md`, `PROJECT-STATE.md` (этот файл) — в процессе создания
- 🔄 `ai/fail_patterns.md` — заготовка

### Auto-memory (системная Claude memory)

- ✅ `MEMORY.md` + 8 файлов-указателей: project, references (memory_bank, server, github, secrets), user_profile, feedback (simple_language, rules_location, docs_canon)

### Доступы

- ✅ Сервер 193.160.208.41 через `~/.ssh/id_ed25519`
- ✅ Telegram bot token (от @BotFather)
- ✅ Replicate API token (от пользователя)
- ✅ OpenAI API key (взят от соседнего проекта med.2opinion.online на том же сервере)
- ✅ OpenAI прокси через хостовой gptproxy: `http://host.docker.internal:8089/gpt`
- ✅ ADMIN_TG_ID: 459255913 (@my_coruja)

### Инфраструктура

- ✅ Изучен сервер: 88% диска занято (свободно 6.1 ГБ), 9 чужих контейнеров (wfm-admin, med-*, sup-*, strapi, gptproxy, ...)
- ✅ Зафиксированы свободные порты (для нас не нужны — long polling)
- ✅ Git установлен через winget (нужен новый терминал чтобы попал в PATH)

## Что ещё в Шаге 0

- ⏳ Завершить дополнение памяти из аудита (4 файла остались)
- ⏳ Инициализация git-репозитория локально
- ⏳ Проверка SSH-доступа к Github (`ssh -T git@github.com`)
- ⏳ Первый commit + push на github.com/julia15535/aiflatdesign
- ⏳ Подготовка папки `/opt/aiflatdesigner/` на сервере и `git clone`

## Что в планах (после Шага 0)

См. `product_roadmap.md` и `plans/`.

- Шаг 1 — скелет бота (FSM, БД, заглушки AI), 4-6 часов
- Шаг 2 — проверка качества фото + анализ сцены (OpenAI), 6-8 часов
- Шаг 3 — поиск места + проверка размеров, 6-8 часов
- Шаг 4 — генерация (FLUX best-of-3) + валидация (CLIP/SSIM), 8-10 часов
- Шаг 5 — бенчмарк на 30 inspiration + 20 catalog, 6-8 часов
- Шаг 6 — туториал (4 ассета) + онбординг, 6-8 часов
- Шаг 7 — beta-тест на 3-5 человек, 4-6 часов
- Шаг 8 (опционально) — доработки по обратной связи, 4-8 часов

## Ключевые принятые решения

См. полные обоснования в `_claude/DECISIONS.md`. Кратко:

1. **Стек**: Python 3.11 + aiogram 3 (long polling) + openai SDK ≥1.50 + replicate SDK + SQLite + Docker. Без Postgres, Redis, webhook — упрощаем для MVP.
2. **OpenAI через прокси**: подключаемся через хостовой gptproxy (общий с med.2opinion.online), переиспользуем ключ.
3. **Развёртывание**: на сервере 193.160.208.41 в отдельном Docker-контейнере, без проброса портов.
4. **Best-of-N**: `n_seeds=3` для FLUX Fill Pro, выбор лучшего по WOW score.
5. **Где работаем**: сразу на сервере (а не локально на Windows-машине пользователя).
6. **Когда пушим на Github**: сейчас — текущее состояние памяти, до начала кода.
7. **Сверка с пользователем**: на ключевых точках (в конце каждого шага), не на каждом изменении.

## Известные ограничения и риски

| Что | Степень | План если случится |
|---|---|---|
| 88% диска занято | средний | `docker system prune -af` перед сборкой |
| Прокси gptproxy без auth — публичный | низкий | Не публиковать IP сервера, ключ ротировать если утечка |
| Общий OpenAI ключ с med-проектом | низкий | При проблемах — взять отдельный ключ |
| FLUX-генерация может "выдумывать" мебель | **средний (главный риск)** | В Шаге 5 проверяем; если catalog_sim < 0.65 — пивот |
| MemoryStorage FSM — теряется при рестарте | низкий | Пользователь делает /start заново; в продукте — Redis |

## Бюджет — статус расходов

| Категория | Бюджет | Потрачено | Остаток |
|---|---|---|---|
| Replicate (FLUX и др.) | $150-220 | $0 | $150-220 |
| OpenAI vision | $15-30 | $0 | $15-30 |
| Visual assets (если генерим) | $1-3 | $0 | $1-3 |
| **Итого** | **$200-280** | **$0** | **$200-280** |

Лимит COST_LIMIT_USD=300 будет в `.env` — авто-стоп при превышении.

## Когда обновлять этот файл

- В конце каждого Шага (когда меняется фаза)
- При большом архитектурном решении (одновременно с записью в DECISIONS.md)
- При смене статуса доступа (новый токен, отозванный ключ)
- При значимом расходе бюджета (>$30 разово или >$50 суммарно)
